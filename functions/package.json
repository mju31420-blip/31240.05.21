{
  "name": "myeong-biseo-functions",
  "description": "명비서 API — 식단 크롤링 + 시간표 OCR",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "lint": "node -e \"const fs=require('fs');const p=require('path');const src=p.join('..','public','data','lecture_db.json');const dst=p.join('data','lecture_db.json');fs.mkdirSync('data',{recursive:true});if(fs.existsSync(src))fs.copyFileSync(src,dst);console.log('ok')\"",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0",
    "nodemailer": "^6.9.16"
  }
}

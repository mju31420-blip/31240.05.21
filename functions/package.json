{
  "name": "myeong-biseo-functions",
  "description": "명비서 API — 식단 크롤링 + 시간표 OCR",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "lint": "node -e \"const fs=require('fs');const p=require('path');const dir=p.join('data');fs.mkdirSync(dir,{recursive:true});['lecture_db.json','class_periods.json'].forEach(f=>{const src=p.join('..','public','data',f);const dst=p.join(dir,f);if(fs.existsSync(src))fs.copyFileSync(src,dst);});console.log('ok')\"",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0",
    "nodemailer": "^6.9.16"
  }
}
